#
# ~/.bashrc
#

## CONDITIONAL CLAUSES THAT PREVENT TO SOURCE BASHRC

	# If not running interactively, don't do anything

		[[ $- != *i* ]] && return


## DECLARING ENVIRONMENTAL VARIABLES

	export ZSH=$HOME"/.oh-my-zsh"


## PROGRAMS OPTIONS

	# GREP

		GREP_OPTIONS='--color=auto'

## ABREVIATING COMMANDS

	alias ls='ls --color=auto'
	alias tmuxa='tmux new-session -t aperture'


## DEFINE PRIMARY PROMPT OUTPUT

	PS1='[\u@\h \W]\ '
	printf '\033[40m\n$ '

